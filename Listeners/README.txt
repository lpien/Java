This assignment utilizes KeyEvents to move around self-created shapes, as well as using an array of color options that may fill the shape.
When running the code, the user can use the arrow keys to move the square around the panel; they can also press
the enter key to change the color of the square, rotating through five different colors.
